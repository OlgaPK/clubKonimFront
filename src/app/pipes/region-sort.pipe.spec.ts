import { RegionSortPipe } from './region-sort.pipe';

describe('RegionSortPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionSortPipe();
    expect(pipe).toBeTruthy();
  });
});
